default: &default
  adapter: postgresql
  pool: 5
  timeout: 5000

development:
  <<: *default
  database: first_app_development

test:
  <<: *default
  database: first_app_test

production:
  <<: *default
  database: first_app_production
